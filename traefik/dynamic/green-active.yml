http:
  routers:
    frontend-router:
      rule: "Host(`yomu.my.id`)"
      service: frontend-green-svc
      entryPoints:
        - websecure
      tls:
        certResolver: letsencrypt

    java-router:
      rule: "Host(`java.yomu.my.id`)"
      service: java-green-svc
      entryPoints:
        - websecure
      tls:
        certResolver: letsencrypt

    rust-router:
      rule: "Host(`rust.yomu.my.id`)"
      service: rust-green-svc
      entryPoints:
        - websecure
      tls:
        certResolver: letsencrypt

    grafana-router:
      rule: "Host(`monitoring.yomu.my.id`)"
      service: grafana-svc
      entryPoints:
        - websecure
      tls:
        certResolver: letsencrypt

    prometheus-router:
      rule: "Host(`monitoring.yomu.my.id`) && PathPrefix(`/prometheus`)"
      service: prometheus-svc
      middlewares:
        - strip-prometheus
      entryPoints:
        - websecure
      tls:
        certResolver: letsencrypt

    http-catchall:
      rule: "HostRegexp(`{host:.+}`)"
      service: traefik-echo
      entryPoints:
        - web

  middlewares:
    strip-prometheus:
      stripPrefix:
        prefixes:
          - /prometheus

  services:
    frontend-green-svc:
      loadBalancer:
        servers:
          - url: "http://frontend-green:3000"

    java-green-svc:
      loadBalancer:
        servers:
          - url: "http://java-green:8080"

    rust-green-svc:
      loadBalancer:
        servers:
          - url: "http://rust-green:8080"

    grafana-svc:
      loadBalancer:
        servers:
          - url: "http://grafana:3000"

    prometheus-svc:
      loadBalancer:
        servers:
          - url: "http://prometheus:9090"

    traefik-echo:
      loadBalancer:
        servers:
          - url: "http://127.0.0.1:9999"
